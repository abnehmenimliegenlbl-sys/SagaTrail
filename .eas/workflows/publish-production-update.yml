name: Publish production OTA update

on:
  push:
    branches: ['main']

defaults:
  tools:
    pnpm: 10.26.1
  run:
    working_directory: ./artifacts/mobile

jobs:
  publish_production_update:
    name: Publish production OTA update
    type: update
    params:
      branch: production